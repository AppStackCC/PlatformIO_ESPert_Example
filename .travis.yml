language: python
python:
    - "2.7"

sudo: false
cache:
directories:
        - "~/.platformio"

env:
    - PLATFORMIO_CI_SRC=src/main.cpp

install:
    - pip install -U platformio
    - platformio lib install 19 28 64 66 89 562

script:
    - platformio ci --board=espresso_lite_v1 
